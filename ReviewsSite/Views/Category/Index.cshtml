@model IEnumerable<ReviewsSite.Models.Category>
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Cats of the Migra</title>
    <link href="~/StyleSheet.css" rel="stylesheet" />
</head>
<body>
    @*needs to not be linked to reviews but category instead*@
    @foreach (var category in Model)
        {
            <div><a href="/Category/Details/@category.Id">@category.Type</a></div>
        }

</body>
</html>
